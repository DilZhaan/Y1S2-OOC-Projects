#include <iostream>
using namespace std;

int main(void) {
	cout << "Hello\n";
	char ch;
	cin >> ch; // Without an input command visual studio
	// runs the code and automatically returns to the editor
	return 0;
}